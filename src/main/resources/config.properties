# ============================================================
# API / Services configuration (NO secrets committed)
# Set these as environment variables on your machine.
# ============================================================

grok.api.key=${GROK_API_KEY}

# Stripe (test/dev)
stripe.secret_key=${STRIPE_SECRET_KEY}

# Brevo / Sendinblue
mail.api.key=${MAIL_API_KEY}
mail.replyTo=${MAIL_REPLY_TO}
mail.fromName=${MAIL_FROM_NAME}