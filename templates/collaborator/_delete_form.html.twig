<form method="post" action="{{ path('app_collaborator_delete', {'idUser': collaborator.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborator.idUser) }}">
    <button class="btn">Delete</button>
</form>
