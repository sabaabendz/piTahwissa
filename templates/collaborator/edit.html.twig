{% extends 'base_admin.html.twig' %}

{% block title %}Edit Collaborator - SmartTask Manager{% endblock %}

{% block body %}
	<!-- Title -->
	<div class="row">
		<div class="col-12">
			<div class="d-flex justify-content-between align-items-center">
				<h1 class="h3 mb-2 mb-sm-0">Edit Collaborator</h1>
				<a href="{{ path('app_collaborator_show', {'idUser': collaborator.idUser}) }}" class="btn btn-light mb-0">
					<i class="bi bi-arrow-left me-2"></i>Back to details
				</a>
			</div>
		</div>
	</div>

	<div class="card shadow mt-4">
		<div class="card-header border-bottom p-4">
			<h5 class="card-header-title mb-0">Collaborator Information</h5>
		</div>
		<div class="card-body p-4">
			{{ include('collaborator/_form.html.twig') }}
		</div>
	</div>
{% endblock %}
