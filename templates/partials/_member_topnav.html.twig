<nav class="navbar navbar-expand-lg fo-topbar border-0 shadow-sm">
    <div class="container-fluid px-3 px-lg-4">
        <a class="navbar-brand fw-bold text-white" href="{{ path('app_dashboard_index') }}">SmartTask</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#memberNavbar" aria-controls="memberNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="memberNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link fo-nav-link {{ app.request.get('_route') == 'app_dashboard_index' ? 'active' : '' }}" href="{{ path('app_dashboard_index') }}">Dashboard</a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link fo-nav-link {{ app.request.get('_route') starts with 'app_admin_users' ? 'active' : '' }}" href="{{ path('app_admin_users_index') }}">Users</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link fo-nav-link {{ app.request.get('_route') == 'app_front_profile_edit' ? 'active' : '' }}" href="{{ path('app_front_profile_edit') }}">Settings</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link fo-nav-link" href="{{ path('app_dashboard_index') }}">Reports</a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    {% set avatarPath = app.user and app.user.avatarUrl ? app.user.avatarUrl : asset('assets/images/avatar/01.jpg') %}
                    <a class="nav-link fo-nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" id="profileDropdownMember" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="{{ avatarPath }}" alt="avatar" class="rounded-circle avatar-hover" width="32" height="32">
                        <span>{{ app.user ? ((app.user.firstName ?: '') ~ ' ' ~ (app.user.lastName ?: '')|trim) : 'Profile' }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdownMember">
                        <li><a class="dropdown-item" href="{{ path('app_front_profile_edit') }}">Profile</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_front_profile_edit') }}">Settings</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>