{% extends 'base_admin.html.twig' %}

{% block title %}Managers - SmartTask Manager{% endblock %}

{% block body %}
	<!-- Title -->
	<div class="row">
		<div class="col-12">
			<div class="d-flex justify-content-between align-items-center">
				<h1 class="h3 mb-2 mb-sm-0">Managers</h1>
				<a href="{{ path('app_manager_new') }}" class="btn btn-primary mb-0">
					<i class="fas fa-plus me-2"></i>Add Manager
				</a>
			</div>
		</div>
	</div>
		
	<div class="card bg-transparent mt-4">
		<!-- Card header START -->
		<div class="card-header bg-transparent border-bottom px-0">
			<!-- Search and select START -->
			<div class="row g-3 align-items-center justify-content-between">
				<!-- Search bar -->
				<div class="col-md-8">
					<form class="rounded position-relative">
						<input class="form-control bg-transparent" type="search" placeholder="Search managers..." aria-label="Search">
						<button class="bg-transparent p-2 position-absolute top-50 end-0 translate-middle-y border-0 text-primary-hover text-reset" type="submit">
							<i class="fas fa-search fs-6"></i>
						</button>
					</form>
				</div>
			</div>
			<!-- Search and select END -->
		</div>
		<!-- Card header END -->

		<!-- Card body START -->
		<div class="card-body px-0">
			<div class="row g-4">
				{% for manager in managers %}
					<!-- Card item START -->
					<div class="col-md-6 col-xxl-4">
						<div class="card bg-transparent border h-100"> 
							<!-- Card header -->
							<div class="card-header bg-transparent border-bottom d-flex justify-content-between">
								<div class="d-sm-flex align-items-center">
									<!-- Avatar -->
									<div class="avatar avatar-md flex-shrink-0">
										<div class="avatar-img rounded-circle bg-primary bg-opacity-10">
											<span class="text-primary position-absolute top-50 start-50 translate-middle fw-bold">{{ manager.name|first|upper }}</span>
										</div>
									</div>
									<!-- Info -->
									<div class="ms-0 ms-sm-2 mt-2 mt-sm-0">
										<h5 class="mb-0"><a href="{{ path('app_manager_show', {'idUser': manager.idUser}) }}">{{ manager.name }}</a></h5>
										<span class="text-body small"><i class="bi bi-envelope-fill me-1"></i>{{ manager.email }}</span>
									</div>
								</div>

								<!-- Edit dropdown -->
								<div class="dropdown text-end">
									<a href="#" class="btn btn-sm btn-light btn-round small mb-0" role="button" data-bs-toggle="dropdown" aria-expanded="false">
										<i class="bi bi-three-dots fa-fw"></i>
									</a>
									<ul class="dropdown-menu dropdown-w-sm dropdown-menu-end min-w-auto shadow rounded">
										<li><a class="dropdown-item" href="{{ path('app_manager_edit', {'idUser': manager.idUser}) }}"><i class="bi bi-pencil-square fa-fw me-2"></i>Edit</a></li>
									</ul>
								</div>
							</div>

							<div class="card-body">
								<!-- Level -->
								<div class="d-flex justify-content-between align-items-center mb-3">
									<div class="d-flex align-items-center">
										<div class="icon-md bg-warning bg-opacity-10 text-warning rounded-circle flex-shrink-0"><i class="fas fa-layer-group fa-fw"></i></div>
										<h6 class="mb-0 ms-2 fw-light">Level</h6>
									</div>
									<span class="mb-0 fw-bold">{{ manager.level ?? 'N/A' }}</span>
								</div>

								<!-- Department -->
								<div class="d-flex justify-content-between align-items-center mb-3">
									<div class="d-flex align-items-center">
										<div class="icon-md bg-info bg-opacity-10 text-info rounded-circle flex-shrink-0"><i class="fas fa-building fa-fw"></i></div>
										<h6 class="mb-0 ms-2 fw-light">Department</h6>
									</div>
									<span class="mb-0 fw-bold">{{ manager.department ?? 'N/A' }}</span>
								</div>
								
								<!-- Enterprise Code -->
								<div class="d-flex justify-content-between align-items-center">
									<div class="d-flex align-items-center">
										<div class="icon-md bg-success bg-opacity-10 text-success rounded-circle flex-shrink-0"><i class="bi bi-key fa-fw"></i></div>
										<h6 class="mb-0 ms-2 fw-light">Enterprise Code</h6>
									</div>
									<span class="mb-0 fw-bold text-primary">{{ manager.enterpriseCode }}</span>
								</div>
							</div>

							<!-- Card footer -->
							<div class="card-footer bg-transparent border-top">
								<div class="d-sm-flex justify-content-between align-items-center">
									<!-- Buttons -->
									<div class="text-end text-primary-hover">
										<a href="{{ path('app_manager_show', {'idUser': manager.idUser}) }}" class="btn btn-sm btn-primary mb-0">
											<i class="bi bi-eye me-1"></i>View Details
										</a>
									</div>
								</div>
							</div>
						</div>
					</div>
					<!-- Card item END -->
				{% else %}
					<div class="col-12">
						<div class="alert alert-info">
							<i class="fas fa-info-circle me-2"></i>No managers found. <a href="{{ path('app_manager_new') }}">Create the first manager</a>.
						</div>
					</div>
				{% endfor %}
			</div>
		</div>
		<!-- Card body END -->
	</div>
{% endblock %}
