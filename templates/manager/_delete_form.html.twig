<form method="post" action="{{ path('app_manager_delete', {'idUser': manager.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manager.idUser) }}">
    <button class="btn">Delete</button>
</form>
